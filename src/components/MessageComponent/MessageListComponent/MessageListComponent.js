import React from 'react'

const MessageListComponent = (props) => {

    return (
        <div>
            <h1>MessageListComponent</h1>
        </div>

    )
};

export default MessageListComponent;